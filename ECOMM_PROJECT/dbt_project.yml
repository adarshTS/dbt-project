name: ECOMM_PROJECT
version: 1.0.0
config-version: 2
profile: ECOMM_PROJECT
model-paths:
  - models
analysis-paths:
  - analyses
test-paths:
  - tests
seed-paths:
  - seeds
macro-paths:
  - macros
snapshot-paths:
  - snapshots
models:
  ECOMM_PROJECT:
    +materialized: view
